---
title: Install bakefile
description: Install bakefile with pip or uv, pin exact versions with the locked extra, plus the optional bakelib helpers.
---

## Requirements

bakefile supports Python 3.10 through 3.14.

## Install

Install via pip:

```bash
pip install bakefile
```

Or via uv:

```bash
uv add bakefile          # as a project dependency
uv tool install bakefile # as a global tool
```

## Exact dependency versions

The commands above float dependencies within their supported ranges. To install the exact, tested set of every dependency instead, use the `locked` extra:

```bash
uv tool install "bakefile[locked]"
```

With pipx or mise:

```bash
pipx install "bakefile[locked]"
mise use -g 'pipx:bakefile[extras=locked]'
```

A mise install created earlier as bare `pipx:bakefile` keeps working but does not gain the extra. Re-run the `mise use` command above to switch.

## Verify the install

```bash
bake --version
```

Installing bakefile gives you two command-line tools:

- **`bake`** - Runs tasks from your `bakefile.py`
- **`bakefile`** - Manages your `bakefile.py` (init, lint, sync, and more)

## Optional: bakelib

**bakelib** is an optional collection of opinionated helpers built on top of Bakebook: preconfigured task [Spaces](/bakelib/spaces), multi-environment support, refreshable caches, and secrets management. Install it with the `lib` extra:

```bash
pip install bakefile[lib]
```

bakefile works without bakelib.
